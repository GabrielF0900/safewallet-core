/mvnw text eol=lf
*.cmd text eol=crlf

# --- Configuração de Equilíbrio do Gráfico ---

# Bloqueia apenas arquivos de fontes, imagens ou travas de pacotes que pesam muito
package-lock.json linguist-vendored
yarn.lock linguist-vendored
pnpm-lock.yaml linguist-vendored
*.svg linguist-vendored
*.png linguist-vendored

# Deixamos *.tsx (React), *.css (Tailwind) e *.sql (PostgreSQL) livres 
# para o GitHub contar o tamanho real deles e exibi-los no gráfico junto com o Java.